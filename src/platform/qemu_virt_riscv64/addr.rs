// use super::config::KERNEL_VADDR_OFFSET;
// use crate::arch::KADDR_OFFSET;

// pub fn kaddr_to_paddr(kaddr: usize) -> usize {
//     kaddr - KADDR_OFFSET
// }

// pub fn paddr_to_kaddr(paddr: usize) -> usize {
//     paddr + KADDR_OFFSET
// }
