interface:
  display_name: "KernelX Qperf Analysis"
  short_description: "Interpret qperf profiles and separate sampling noise"
  default_prompt: "Use $kernelx-local-test-qperf to interpret an existing KernelX qperf report, separate sampling noise, and trace real hotspots to source."
